name: Create Archive
on: [push]
jobs:
  build:
    runs-on: x64
    steps:   
      - name: Zip Release  
      uses: TheDoctor0/zip-release@0.7.6
      with:
        type: 'zip'
        filename: 'Spectrum.zip'
        exclusions: '*.md* LICENSE .github/*'
